#include "main.h"

/**
 * _isupper - check if letter is uppercase
 * @c: to be checked
 * Return: 1 for uppercase otherwise 0
 */
int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	{
	return (1);
	}
	return (0);
}
